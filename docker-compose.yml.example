version: '3.8'

services:
    php:
        build:
            dockerfile: docker/Dockerfile
            context: .
        volumes:
            - .:/app
        links:
            - rabbitmq

    rabbitmq:
        image: bitnami/rabbitmq:3.7
        ports:
            - 5672:5672
            - 15672:15672
        # volumes:
        # - ${BACKUP_PATH}/rabbitmq/amqp-package:/bitnami
